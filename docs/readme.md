WTF is this where are docs?
This is a minimal PWA that redirects to the main project running on my rasperry pi, but in `/docs` so that in can be served on github pages, which provides sll, which makes it installable.
